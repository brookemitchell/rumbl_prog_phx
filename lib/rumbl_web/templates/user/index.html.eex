<h1>Listing users</h1>

<table>
  <%= for user <- @users do %>
    <tr>
      <td>(<%= user.id %>) <b><%= first_name(user) %></b>  <%= user.username %></td>
      <td><%= link "View", to: Routes.user_path(@conn, :show, user.id) %></td>
    </tr>
  <% end %>
</table>
